[user]
  name = Nick Butler
  email = nick@codeindulgence.com
  signingkey = 08F7FFB6
[core]
  excludesfile = ~/.config/git/ignore
  pager = diff-so-fancy | less --tabs=4 -RFX
  editor = nvim +start
[color]
  ui = true
[branch]
  autosetupmerge = always
[push]
  default = current
[alias]
  co = checkout
  ci = commit
  st = status -bsu
  check = "!git fetch -a && git st"
  ch = "!git check"
  ignore = "!gi() { curl -L -s https://www.gitignore.io/api/$@ ;}; gi"
  pop = stash pop
  prb = "!git stash; git pull --rebase; git stash pop"
[credential]
  helper = osxkeychain
[diff]
  compactionHeuristic = true
